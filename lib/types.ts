export type ProductType = {
    currentItem: string
    products: [
        {
            product: {
                
                handle: string
                title: string
                image: {
                    url: string;
                };
            }
        }
    ]
}
